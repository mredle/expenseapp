meta {
  name: Remove Event User
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/events/{{event_guid}}/users/{{eventuser_guid}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
