meta {
  name: Get Settlement
  type: http
  seq: 3
}

get {
  url: {{base_url}}/events/{{event_guid}}/settlements/{{settlement_guid}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
