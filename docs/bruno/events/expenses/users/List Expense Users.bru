meta {
  name: List Expense Users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/events/{{event_guid}}/expenses/{{expense_guid}}/users?page=1&per_page=25
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
