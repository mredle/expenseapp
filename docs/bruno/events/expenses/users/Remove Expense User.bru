meta {
  name: Remove Expense User
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/events/{{event_guid}}/expenses/{{expense_guid}}/users/{{eventuser_guid}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
