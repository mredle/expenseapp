meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/users/
  body: json
  auth: bearer
}

body:json {
  {
    "username": "newuser",
    "email": "new@example.com",
    "password": "password123",
    "locale": "en"
  }
}
